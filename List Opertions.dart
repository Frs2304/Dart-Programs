void main() {
  List first = [1, 2, 3, 4, 5];
// Remove operation removes the elemnt given in the input
  first.remove(2);
  print(first);
// Empty Funtion to check whether the list is empty or not
// returns true if list is empty else returns false
  print(first.isEmpty); 
}
